%YAML:1.0

#--------------------------------------------------------------------------------------------
# RealSense D435i RGB-D Camera Parameters
#--------------------------------------------------------------------------------------------
File.version: "1.0"

Camera.type: "PinHole"

# D435i RGB Camera calibration and distortion parameters (OpenCV)
# These are typical values for D435i, but should be calibrated for your specific camera
Camera1.fx: 382.613
Camera1.fy: 382.613
Camera1.cx: 320.183
Camera1.cy: 236.455

# Distortion parameters (D435i typically has minimal distortion)
Camera1.k1: 0.0
Camera1.k2: 0.0
Camera1.p1: 0.0
Camera1.p2: 0.0
Camera1.k3: 0.0

# Camera resolution (D435i default)
Camera.width: 640
Camera.height: 480

# Camera frames per second (D435i supports 30fps)
Camera.fps: 30

# Color order of the images (0: BGR, 1: RGB. D435i typically uses RGB)
Camera.RGB: 0

#--------------------------------------------------------------------------------------------
# RGB-D specific parameters for D435i
#--------------------------------------------------------------------------------------------

# Depth scaling factor for D435i
# D435i depth is in millimeters, so factor is typically 1000.0
# Adjust this based on your depth image format and units
RGBD.DepthMapFactor: 1000.0

# Close/Far threshold for depth filtering
# D435i has good depth range, so we can use a wider threshold
Stereo.ThDepth: 40.0

# Stereo baseline (required for RGB-D mode)
# For D435i, this is the baseline between RGB and depth cameras
Stereo.b: 0.05

#--------------------------------------------------------------------------------------------
# ORB Parameters (optimized for D435i)
#--------------------------------------------------------------------------------------------

# ORB Extractor: Number of features per image
# D435i has good image quality, so we can extract more features
ORBextractor.nFeatures: 1250

# ORB Extractor: Scale factor between levels in the scale pyramid 	
ORBextractor.scaleFactor: 1.2

# ORB Extractor: Number of levels in the scale pyramid	
ORBextractor.nLevels: 8

# ORB Extractor: Fast threshold
# D435i has good contrast, so we can use standard thresholds
ORBextractor.iniThFAST: 20
ORBextractor.minThFAST: 7

#--------------------------------------------------------------------------------------------
# Viewer Parameters
#--------------------------------------------------------------------------------------------
Viewer.KeyFrameSize: 0.05
Viewer.KeyFrameLineWidth: 1.0
Viewer.GraphLineWidth: 0.9
Viewer.PointSize: 2.0
Viewer.CameraSize: 0.08
Viewer.CameraLineWidth: 3.0
Viewer.ViewpointX: 0.0
Viewer.ViewpointY: -0.7
Viewer.ViewpointZ: -1.8
Viewer.ViewpointF: 500.0 